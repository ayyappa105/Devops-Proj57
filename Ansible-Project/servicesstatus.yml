- name: gathering the bullikaya
  hosts: all
  tasks:
   - name: gathering the facts
     service_facts:

   - name: pritning the service status
     ansible.builtin.debug:
       msg: "{{value.state == running}}"
  
       
  